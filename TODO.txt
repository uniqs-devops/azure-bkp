# Prioridad Alta

- "resourceGroup": debe soportar mas de un resourceGroup, se utiliza en los scripts de backup
	EJ.  "resourceGroup": "cibd1weursgglocolcrit001, cibd1weursgglocolcrit002"
	Asumir que cada resourceGroup tiene un keyVaut o que todos los rg usan un solo keyvault
	

# Prioridad media

- Si "proxy": {
    "useProxy": "YES"
    
    Hacer que docker build en dps-setup.sh aceptarte esto desde una clave json del grupo "proxy" para IP y un parsing para "proxy.proxyHttpName"

    --add-host proxy.threatpulse.net:98.158.252.164
    	
- Sincronizar este repo con GitLab para permitir incrustar imagen de CentOS y evitar salir a internet con git clone.


# Prioridad baja

- Verificar si corriendo una nueva instancia del contenedor se pueden reconfigurar los clientes de Avamar y Postgres para distribuir una imagen.

- Poner una clave nueva en dps-setup.json permitir dos tipos de login
  
  Para santander az login --service-principal --username --password --tenanid, es decir deben p

  Para otros  az login --service-principal --username http://PaaSBackup --password  --tenant 
  
- La clave "installDir.dockerclient" debe salir de "avamar.avamarServerName" debe ubicarse en el nivel superior, hay que modificar los .sh de configuracion y backup.

- Revisar dps-setup.sh -d <hostname>

- Las claves "datadomainServerName.mountType" y "datadomainServerName.RootBackupDir" deben ubicarse en el nivel superior, hay que modificar los .sh de configuracion y backup.

- Cambiar $KeyVault en backup-keyvault.sh por valor aprendido de la lista

  az keyvault secret backup --file ${BackupDir}/$KeyVault.$1.$(date +%Y%m%d%H%M%S).bkp --vault-name $KeyVault --name $1

